The test of LineClipping is performed by the ClippingTest.cs script; its results appear in the console

The test of the rasterisation is performed RasterisationTest script which draw and rotate a cube on a plan.